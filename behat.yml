default:
  extensions:
    Behat\MinkExtension\Extension:
      base_url: http://trunk.lemma.jsn.dev.ordbogen.com
      selenium2: ~
      default_session: selenium2 
  paths:
    features:  features
    bootstrap: features/bootstrap

annotations:
  paths:
    features: features/annotations

closures:
  paths:
    features: features/closures
